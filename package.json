{
  "name": "playmaster-auto-import",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Recepção, identificação e preparação de áudios do ecossistema PlayMaster.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/store.js && node --check src/importer.js"
  },
  "engines": {
    "node": ">=20"
  }
}
