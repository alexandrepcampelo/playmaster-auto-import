{
  "name": "playmaster-auto-import",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Recepção, identificação e preparação de áudios do ecossistema PlayMaster.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/auth.js && node --check src/upload.js && node --check src/archive-upload.js && node --check src/store.js && node --check src/library.js && node --check src/audio-processor.js && node --check src/importer.js && node --check public/app.js && node --check agent/src/main.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yauzl": "^3.2.0"
  }
}
